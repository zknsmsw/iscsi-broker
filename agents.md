# agents.md

本文件为本仓库内 AI / 代码代理（agent）协作时必须遵守的规则。违反以下规则视为未完成交付。

## 1. 新功能必须提交并推送

- 任何新功能（feature）开发完成后，**必须**执行 `git commit` 并 `git push` 到远端，才算完成该功能交付。
- 提交信息须简洁、描述清楚本次变更内容（建议遵循 Conventional Commits，如 `feat: ...`、`fix: ...`、`test: ...`）。
- 不得留下未提交（uncommitted）的功能代码，或只 commit 不 push。

## 2. 测试必须先通过才可交付

- 每次交付前，**必须**为相关改动编写 / 更新测试用例（`test_*.py`），覆盖新功能及受影响逻辑的正常与异常路径。
- **必须**运行全部相关测试，确认所有测试用例通过（无失败、无报错）后，才能交付。
- 若测试失败，必须修复代码或测试本身直至通过；禁止以"跳过测试"或"忽略失败"的方式交付。
- 建议在交付前执行完整测试命令，例如：

  ```bash
  python -m unittest discover -s . -p "test_*.py" -v
  ```

  （如项目使用 pytest，则使用 `pytest -v`，以实际项目配置为准。）

## 交付检查清单

- [ ] 新功能代码已编写并有相应测试用例
- [ ] 全部相关测试运行通过
- [ ] 已执行 `git commit`（提交信息清晰）
- [ ] 已执行 `git push` 推送到远端
